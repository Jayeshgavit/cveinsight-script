# Direct dependencies only — let pip resolve transitive deps
# Pinning every transitive dep from pip freeze causes platform conflicts

# Supabase client (requires postgrest, gotrue, storage3, realtime)
supabase>=2.5.0

# AI providers
openai>=1.50.0          # used for Groq's OpenAI-compatible API
google-genai>=1.0.0     # Gemini via google-genai SDK

# HTTP client (google-genai needs >=0.28.1; gotrue needs <0.29)
httpx>=0.28.1,<0.29.0

# Utilities
python-dotenv>=1.0.0
rich>=13.0.0
